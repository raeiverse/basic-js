// we can renaming the Name in the receiving file
import prs from "./person";

// same exact same text (name) to use 
import { baseData } from "./utility";

// using as to alternative calling function to use
import { baseData as data } from "module";