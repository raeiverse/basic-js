const numbers = [1,2,3];

// map is to catch array data and (names) is variable to call method
const doubleNumbers = numbers.map((num)=>{
    return num * 2;
});

console.log(numbers);
console.log(doubleNumbers);